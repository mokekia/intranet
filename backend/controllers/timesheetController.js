// getMyShifts: returns logged-in employee's shifts filtered by month query param
// getPendingShifts: returns all shifts with status Pending (manager/admin only)
// getAllShifts: returns all shifts with optional filters (manager/admin only)
// createShift: employee logs a new shift for a given date
// updateShiftStatus: manager approves or rejects a shift by id
