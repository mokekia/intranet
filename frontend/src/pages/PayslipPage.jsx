// Payslip page matching the design:
// - "PAYSLIP" heading
// - Batch Summary card (dark navy): Current Period, Hours Worked, Gross Salary, Net Salary, Tax Deducted — fetched from GET /api/payslip/current
// - Previous Payslips table (dark navy): columns Month, Net Salary, Hours Worked — fetched from GET /api/payslip/history

function PayslipPage() {
  return(
    <div>
      Payslip Page
    </div>
  )
}
export default PayslipPage