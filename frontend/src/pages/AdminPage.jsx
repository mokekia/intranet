// Admin page matching the design (Grade 5 — admin dashboard):
// - "ADMIN" heading
// - Admin Credentials card: shows logged-in admin's name, email, gross salary, role
// - Employees table: columns Name, Email, Hourly Rate, Role (green = employee, red = manager) with three-dot menu per row to edit or delete
// - Add Employee form at the bottom: Name, Email, Password, Hourly Rate, Role inputs + "Add Employee" button
// - Data visualization section (Grade 5): bar/line chart of total approved hours per month, built with recharts — data from GET /api/admin/stats
